import { LoginCard } from "../components/LoginCard";
import { Navbar } from "../components/Navbar";

export const Login = () => {
  return (
    <div>
      <Navbar />
      <LoginCard />
    </div>
  );
};
